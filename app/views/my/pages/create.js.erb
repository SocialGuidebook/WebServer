<% if @page.errors.empty? %>
  $("#posts").prepend("<%= escape_javascript(render :partial => 'my/dashboard/post', :locals => {:page => @page}) %>");
  $("#post_form").reset();
<% else %>
  $("#new_form").html("<%= escape_javascript(render :partial => 'my/dashboard/new_form') %>");
<% end %>
